# TODO / deferred work

Every deferral, approximation, shortcut, or open question found while
implementing goes here. Nothing is dropped silently.

## Where things stand

Every client-side section of MS-PSRP is implemented and tested; see
`SPEC-COVERAGE.md`. The deviations from the specification text were each
researched against the PowerShell sources and against a live server, and the
findings are in the rows below. Three of the five entries turned out to rest on
reasoning that was wrong or unverified, and have been corrected rather than
quietly kept.

What remains genuinely divergent is narrow: accepting protocolversion 2.3 and
above (PSRP-11), and two places where the specification asks for something no
WSMan client can do -- an empty rsp:Command (PSRP-08) and a mustComply OptionSet
on wxf:Connect (PSRP-25). In both of those Microsoft's own client behaves as
this one does, and in both the field is never read by the server.

The entries still open are not missing protocol work. PSRP-28 records a
constraint on any future move to protocol 2.4. PSRP-03 and PSRP-05 exist only to
support a non-Windows port, which would mean replacing the transport and the XML
backend together; neither affects correctness against a Windows server.

## Rules

- Each entry gets a stable id `PSRP-nn`. Code references it as `/* TODO(PSRP-nn) */`.
- An entry records: what, which spec section, why deferred, and what "done" means.
- Entries are only closed when the behaviour is implemented **and** tested.
- **Before declaring any phase or the project done**, re-read this file top to
  bottom and reconcile it against `SPEC-COVERAGE.md`. Any `deferred` row in the
  coverage file must have a matching open entry here, and vice versa.

## Status legend

`open` — not started · `wip` — in progress · `closed` — done + tested · `wontfix` — agreed out of scope

---

## Open

| id | Area | Spec | Item | Why deferred | Status |
|---|---|---|---|---|---|
| PSRP-01 | Compression | 2.1 | Xpress / MS-XCA compression of WSMan payloads | Closed: nothing to implement. wsman.h documents compression as on by default for Send and Receive, with WSMAN_FLAG_NO_COMPRESSION to turn it off; the transport passes 0 for those flags, so payloads are already compressed by the WSMan client. MS-XCA lives below the PSRP layer, and a hand-rolled HTTP transport (PSRP-03) would be the only thing that had to implement it. | closed |
| PSRP-05 | XML | n/a | XmlLite is a Windows-only XML backend | Closed. The pull-parser seam in `src/internal/psrp_xml.h` now has two backends: XmlLite on Windows and libxml2's xmlTextReader elsewhere, both passing the same CLIXML tests. The one thing worth recording is where the backends had to be made to agree rather than merely both work: XmlLite collects an element's attributes up front, so several psrp_xml_attr results are valid simultaneously, and clixml_read.c relies on that when it holds `N` and `RefId` together. A libxml2 backend returning one reusable buffer would have corrupted one of them, silently. The seam was written before either of these existed and did not need changing, which is the useful evidence about it. | closed |
| PSRP-03 | Portability | n/a | The library is Windows-only | Open, narrowed to one thing: the transport. The protocol core, XML (libxml2), RNG and now crypto (OpenSSL) all build and pass their tests on Linux in CI -- 24/24, including the full crypto suite, which runs against both backends because it pins wire formats where a reasonable-looking choice fails to interoperate silently rather than loudly. Splitting crypto_shared.c out first meant the second backend implemented six primitives instead of reimplementing twelve functions, and the CLIXML shape of a PUBLIC_KEY message now cannot drift between platforms because only one copy of it exists. One real behavioural difference surfaced: OpenSSL 3 applies implicit rejection to PKCS#1 v1.5, returning a pseudorandom plaintext instead of a padding error to deny a Bleichenbacher oracle, so a bad ciphertext reaches the length check as a successful decrypt where CNG fails outright; both now report PSRP_ERR_CRYPTO so the observable behaviour matches. What remains is WSMan: HTTP plus SPNEGO/Kerberos/NTLM and message encryption, which Windows hands us free through WsmSvc and which libcurl plus GSSAPI would have to replace. psrp_client and the transport stay absent from a non-Windows build rather than stubbed. The wchar_t in the config structs is the API decision that travels with that work. Not done: the session key-exchange cases still run on Windows only, because their server half is written against CNG; porting that helper would re-test mostly platform-free code, so it is deliberately low priority. | open |
| PSRP-04 | Crypto | 2.2.5.1.24 | SecureString encryption requires session key exchange | Implemented: RSA key exchange plus AES-256-CBC. The zero IV is inferred, not specified: 2.2.5.1.24 names the algorithm and mode but no IV, and the wire carries nowhere to put one, so a fixed zero IV is the only interoperable reading. Not yet confirmed against a live exchange. | closed |
| PSRP-07 | HostInfo | 2.2.3.14 | Populated `_hostDefaultData` dictionary (colors, coordinates, sizes, window title) | Implemented: all ten required keys with their Color / Coordinates / Size / Int32 / String types. A null host still omits the dictionary entirely, which is what the spec's own null-host example shows. | closed |
| PSRP-08 | Transport | 3.1.5.3.3 | rsp:Command is a single space, not empty | Closed as a spec defect the reference implementation shares. The spec says the Command element MUST be empty, but no WSMan client can obey it: WSManRunShellCommand and its Ex form reject "" and NULL in client-side parameter validation with 0x80338180, sub-millisecond, no handle and no request sent, under every flag and with or without an arg set; any non-empty string passes, including a lone tab. Microsoft's client carries the same workaround verbatim -- WSManTransportManager.CreateAsync passes (_cmdLine == null || _cmdLine.Length == 0) ? " " : ... above the comment "WSManRunsShellCommand doesn't accept empty string" -- and normally diverges further by putting the pipeline's command text (truncated to 255 chars) in the field. The space is inert: it serializes as a literal 0x20 in <rsp:Command> </rsp:Command>, and pwrshplugin never reads the field at all. Not matching PowerShell's command-text behaviour is deliberate: it is ignored by the server and would leak pipeline text into WinRM's logs. | closed |
| PSRP-09 | Host calls | 2.2.6 | Typed decoding of host method parameters (mp) | Closed: the 2.2.6.1 encodings are implemented. Parameters are reachable by index, T/V wrappers unwrap to their value, plain values pass through unchanged as 2.2.6.1.1 requires, and arrays expose their flattened elements and dimension sizes. What remains undone is mapping each host *method* signature onto typed arguments, which is a job for whatever implements an interactive host, not for the protocol library. | closed |
| PSRP-10 | Metadata | 2.2.3.23 | Full ParameterMetadata detail (type, aliases, switch flag, mandatory) | Closed: each parameter's type name, aliases, switch flag and dynamic flag are read alongside its name, index-aligned with the names array so a caller can walk both together. A parameter whose metadata object is missing still gets an entry carrying its name, so one odd parameter does not cost the caller the rest. There is no Mandatory property in 2.2.3.23; it belongs to the parameter *set* metadata, which the spec does not define a wire type for. | closed |
| PSRP-11 | Negotiation | 3.1.5.4.1.2 | Version table is not enforced literally | Closed, with the original rationale corrected: it was wrong. The check accepts protocolversion major 2 minor >= 1, requires SerializationVersion 1.1.0.1 when present, and ignores PSVersion. Two of those three the spec already permits: its own hedge says that when protocolversion is 2.1 or 2.2 the client SHOULD reach NegotiationSucceeded even if another row mismatches and MAY go Broken, so ignoring PSVersion is licensed and breaking on a foreign SerializationVersion exercises the MAY. Only accepting 2.3+ is a real divergence. The entry previously claimed PowerShell 5.1 announces PSVersion 5.1 and that the strict table would break every modern server. Both are false, and neither was ever measured. RemoteSessionCapability hard-codes new Version(2, 0) for client and server alike; PSVersion is 2.0 on the wire from every PowerShell. And a server echoes the client's protocolversion whenever it is 2.0-2.3, so against our 2.2 offer this server answers 2.2; patching the offer to 2.4 revealed its true ceiling of 2.3. The relaxation is therefore defensive, not load-bearing. Not tightened: a PSVersion check would match MS's client but buy nothing the spec requires, and bounding the minor would only break against a future server. | closed |
| PSRP-12 | Discovery | 3.1.4.10.1 | Enumerating disconnected shells and commands on a server | Closed: implemented via IWSManSession::Enumerate. The earlier note here was wrong. The flat WSMan C API has no enumerate entry point, but the WSMan *automation* interface does, and it is the same one `winrm enumerate` uses. Live-verified: the enumeration lists our own open pool with a matching ShellId. Supplying credentials needs WSManFlagCredUsernamePassword, which the C API infers from the authentication struct and the automation API makes you state. | closed |
| PSRP-13 | Error handling | 3.1.7, 3.1.5.1 r3, 3.1.1.2.2 | Pool goes Broken, not Closed, on a processing error | Closed, and it was never a divergence. 3.1.1.2.2 defines Closed only as the outcome of Closing -> a successful wxf:DeleteResponse; there is no error transition into Closed. "Closed as specified in 3.1.1.2.2" names the closing procedure -- stop pipelines, send wxf:Delete -- and that procedure itself ends in Broken whenever the shutdown is not clean. 3.1.5.3.13 uses the same shape for a fault: Broken plus Delete. PowerShell agrees: RemoteRunspacePoolInternal.HandleSessionClosed sets RunspacePoolState.Broken for any close not initiated from Closing, commented "this close is happening because of data structure handler error"; only a user-initiated close reaches Closed. What was actually missing was the other half of the procedure: notify_fault now fails every pipeline still in the table, as PowerShell does, so a caller waiting on a terminal pipeline event is not stranded. The Delete remains the transport's job (psrp_transport_close_shell / psrp_transport_free), which 3.1.5.1 rule 5 permits after Broken, because the session performs no I/O. | closed |
| PSRP-14 | Transport | -- | WinHTTP holds a connection Event per discarded session for about 60s | Closed, and the original entry was wrong in every particular. It claimed WSMan leaks a process handle per discarded session and that CoUninitialize does not reclaim it. The handles are Event objects created inside webio.dll, WinHTTP's I/O layer, identified by hooking CreateEvent* and attributing live handles to their creating stack: 60 of 62 came from one WinHTTP stack, with no growth in any other object type. The whole three-way signature reproduces in 40 lines of plain WinHTTP with no WSMan, no COM and none of this library's code, so blaming WSMan was misattribution, and the CoUninitialize claim was doubly wrong since COM is not involved. Above all they are not leaked: a scavenger pass at about 60s releases them, to below the count the process started with, and growth is bounded by rate x 60s rather than by session count -- at 6000 iterations the curve bends exactly where the first scavenge falls due and flattens. The original 100, 500 and 1000 iteration runs all finished inside a single scavenge interval, which is why they looked linear; they measured a transient and it was extrapolated into an unbounded leak. An audit for unreleased handles in the library found the normal path clean. psrp_discovery_t and transport reuse stay, as an efficiency measure rather than a workaround for a platform defect. | closed |
| PSRP-15 | Transport | 3.1.5.3.7 | Intermittent failure when cycling transports in a tight loop | Closed: root-caused to a race in the Receive cancellation. Retargeting Receive cancels the in-flight operation, which reports ERROR_OPERATION_ABORTED, and a single expect_abort flag marked that as ours. But cancellation is asynchronous: the completion can arrive after the replacement Receive has been posted and the flag cleared, and the abort is then recorded as a server fault. Replaced with a count of outstanding cancellations that only a consuming callback decrements, and which the receive reset deliberately leaves alone. Reproduced at cycles 41, 48 and 72 before the fix; 400 consecutive cycles clean after it. Guarded by tests/interop/test_stress.c. | closed |
| PSRP-16 | Pipeline input | 2.2.2.10 | NoInput was always true, so sent input was silently discarded | Closed: psrp_session_pipeline_payload used the CREATE_PIPELINE defaults unconditionally, and those set NoInput=true. A server told that closes the input stream on receipt, so psrp_session_send_input could never work through the session's own pipeline creation: the two public functions were unusable together. The failure was silent -- the script just saw an empty $input. Callers now state their intent with PSRP_PIPELINE_EXPECT_INPUT or PSRP_PIPELINE_NO_INPUT. Found only because the input direction was finally run against a live server. | closed |
| PSRP-17 | Metadata | 2.2.2.14 | GET_COMMAND_METADATA always sent a Null Namespace | Closed: the Namespace list is now a parameter on both the builder and the session sender. That was the missing piece, not a server quirk: with Null, "Get-Ch*" returned nothing; naming Microsoft.PowerShell.Management makes the same wildcard find Get-ChildItem. Null still works for "*" and for exact names, so callers who do not care lose nothing. | closed |
| PSRP-18 | RefId | 2.2.5.2.1.1 | Object and type identifiers were not unique within a document | Closed: 2.2.5.2.1.1 says an identifier MUST be unique for the lifetime of a serializer, which 2.2.5.3.3 makes one message. Every builder numbered from zero because a nested builder cannot know what the enclosing document has used, so real messages went out with several different objects all claiming RefId 0, and likewise for type identifiers. The serializer now owns both id spaces and assigns them as it writes; type identifiers are remapped through a table so a TNRef still lands on the TN that declared it. Found by dumping a HostInfo payload while chasing something else. | closed |
| PSRP-19 | Host calls | 2.2.3.14 | A client could not declare a host, so host calls never arrived | Closed: every HostInfo the session sent said _isHostNull, and a server told that runs host methods against its own default host. No RUNSPACEPOOL_HOST_CALL or PIPELINE_HOST_CALL could ever arrive, so psrp_session_respond_to_host_call and the whole 2.2.6 parameter-decoding path were unreachable, in the same silent way as the NoInput bug. psrp_session_provide_host now declares one, for the pool and for the pipelines created after it. Live-verified with $Host.UI.WriteLine. Note Write-Host does not reach the host at all since PowerShell 5.0; it writes to the information stream. | closed |
| PSRP-20 | Metadata | 3.1.5.4.14 | GET_COMMAND_METADATA had no way to be sent or read | Closed: the builder existed but nothing wired it into the session, and the result parsers took raw XML while results arrive as already-deserialized pipeline output. Both halves were unreachable. Added psrp_session_command_metadata_payload, which sends it as a pipeline per 3.1.5.4.14, and value-taking parsers that the XML ones now call. | closed |
| PSRP-21 | Transport | 3.1.5.3.7 | One Receive retargeted per command lost every pool-level message after the first pipeline | Closed: the transport kept a single Receive, retargeted from the shell to each command in turn and never returned to the shell, so once any pipeline had run no RUNSPACE_AVAILABILITY, USER_EVENT or RUNSPACEPOOL_HOST_CALL could arrive. Found by asking for available runspaces after a pipeline instead of before, which is the only order the tests had used. There are now two channels, a shell-level one that lives with the shell and a command-level one per pipeline, which is what PowerShell's own client does. Reconnect now re-posts both explicitly, as 3.1.4.10.2 step 3 requires; before, it only cleared flags and relied on a later run_command to post one. | closed |
| PSRP-22 | Crypto | 2.2.5.1.24 | No public way to build or read a SecureString value | Closed: psrp_crypto_encrypt returns raw AES bytes while an <SS> element carries base64, and the only base64 encoder was internal, so a caller could not produce a SecureString that serialized at all. A value built from the raw bytes is not valid XML and CREATE_PIPELINE simply failed to build. Added psrp_crypto_protect_string and psrp_crypto_unprotect_value, the wire form in both directions. Live-verified both ways: the server decrypts what we protect, and we decrypt what it returns. | closed |
| PSRP-23 | Session | 3.1.4.10.3 | The connect flow could never reach Opened | Closed: after a Connect, step 6 has the client itself move the pool to Opened when SESSION_CAPABILITY arrives; the server sends no RUNSPACEPOOL_STATE for a connect. The session treated it like an open and sat in NegotiationSucceeded waiting for a message that never comes. It now knows which flow it is in. Found only by connecting to a live pool; no earlier test had. | closed |
| PSRP-24 | Transport | 3.1.5.3.14, 3.1.5.3.15 | wxf:Connect had four defects, each masking the next | Closed. The open content was wrapped in <creationXml>; 3.1.5.3.14 requires <connectXml>, and the plugin threw a .NET exception (0xE0434352) when its payload was not there. The ShellId was sent lower-case; the server matches it case-sensitively on Connect and reported the pool enumeration had just listed as not found (0x8033805B). The response's open content was discarded, yet 3.1.5.3.15 puts the server's SESSION_CAPABILITY there and nowhere else, so negotiation could never complete; it is now captured, decoded and handed back, and it turns out to be PSRP fragments, resolving the spec's ambiguity. And no Receive was ever posted after a Connect. With all four fixed, a second client adopts a disconnected pool and runs a pipeline in it. | closed |
| PSRP-25 | Transport | 3.1.5.3.14 | No OptionSet on wxf:Connect | Closed as a spec defect, with the cause corrected: it is mustComply, not the option and not its spelling. Any option passed to WSManConnectShell with mustComply TRUE stays on the shell handle, is re-sent on the next operation, and the server rejects that Receive with 0x80338039. Measured across 13 live runs: fatal for protocolversion 2.2, for 2.1, for 9.9 and for an invented option name alike, and equally fatal spelled ProtocolVersion as the spec's Connect table spells it, so name and case are irrelevant. mustComply FALSE always works, but that is no longer the option the spec asks for, and the server is not reading it anyway -- protocolversion 9.9 and a bogus option name both pass the Connect unchallenged. mustComply TRUE with optionsMustUnderstand FALSE is rejected on the Connect itself. So no spelling satisfies 3.1.5.3.14 and functions. Microsoft's client does the same: ConnectAsync passes IntPtr.Zero for the Connect option set, only CreateAsync builds one, and server-side WSManPlugin.EnsureOptionsComply is reachable only from the create path. A useful by-product: PS_STARTUP_PROTOCOL_VERSION_NAME is "protocolversion" lowercase, confirming the spelling we send on Create. | closed |
| PSRP-26 | Transport | 3.1.4.10.2 | Reconnect reattached the shell but not its command | Closed: WSManReconnectShell does not reattach commands; output a pipeline produced while disconnected stays buffered against the command until WSManReconnectShellCommand is called, which the transport never did. A reconnect after a busy disconnect came back to silence. PowerShell's client reconnects each command after the shell for this reason, and so does this now. Live-verified: a pipeline started before a disconnect delivers its output after the reconnect. | closed |
| PSRP-27 | Session | 3.1.5.3.5 | Outgoing bytes for different pipelines shared one queue | Closed as a latent-correctness fix, not an observed failure. The session held one outgoing buffer and one priority buffer, so a caller running two pipelines at once had both pipelines' input concatenated in take_output with no way to tell which command each belonged to. The bundled WSMan transport runs one command at a time and never hit it, but the sans-IO layer should not force that limitation on a transport that does not share it. Bytes are now queued per destination (pool or pipeline, ordinary or priority stream); take_output still drains everything pool-first for the one-at-a-time case, and take_output_for / next_pending expose the per-destination view a concurrent transport needs. Unit-tested; not driven against a server, because the in-repo transport cannot run two live pipelines to exercise it honestly. | closed |
| PSRP-28 | Negotiation | 2.2.2.1 | Protocol 2.4 deprecates the session key exchange | Not a defect today, a constraint on a future change. RemotingConstants in PowerShell lists 2.4 as PowerShell 7.6 and ProtocolVersionCurrent, and 2.4 makes PUBLIC_KEY, PUBLIC_KEY_REQUEST and ENCRYPTED_SESSION_KEY obsolete when both ends negotiate it. This client announces 2.2, so a server echoes 2.2 and the exchange stays correct. If the announced version is ever raised to 2.4 or above, the key exchange must become conditional on the negotiated version or the two ends will disagree about how a SecureString is protected. Recorded so the coupling is not rediscovered the hard way. | open |
| PSRP-29 | Transport | 3.1.5.3.14 | A failed Connect dropped its shell handle without closing it | Closed. psrp_transport_connect set t->shell to NULL on any failure, but WSManConnectShell can produce a handle even when the operation as a whole fails, and the connectResponseXml checks run after a successful connect. Nulling it stranded the shell: psrp_transport_free could no longer see it and the server held the pool until its idle timeout. The handle is now closed before being cleared. Found while auditing the transport for the handle growth of PSRP-14, which turned out to be unrelated. | closed |
| PSRP-30 | Packaging | n/a | The MS-PSRP specification was redistributed in full | Closed. The document's own IP notice permits copying it to develop an implementation and distributing *portions* to document one; shipping the whole 5.3 MB copyrighted PDF in a public repository is neither, while the section citations throughout `SPEC-COVERAGE.md` and the source comments clearly are covered. `reference/doc/` is untracked and gitignored, the local copy stays for grepping, and the history was rewritten with git filter-repo before the repository was published, so the blobs are gone from every commit rather than merely from the tip. All 50 commits survived; the git directory went from 5.6 MB to 554 KB. | closed |
| PSRP-31 | CI | n/a | The live interop suite on a hosted runner was unproven | Closed, and it works. A GitHub-hosted windows-latest runner permits Enable-PSRemoting on itself, and the interop suite connects to localhost:5985 as the current user over Negotiate, so no secret is stored. Verified on ci run 2: the job's steps each report success individually, including `Run the interop suite`, which matters because continue-on-error masks a job's outcome but not its steps -- a green job alone would not have been evidence. ctest also fails when a label matches nothing, so the run could not have passed vacuously. continue-on-error is removed and the job now gates like any other, meaning the 16 live feature sections run on every push. One property to keep in mind: Enable-PSRemoting configures an HTTP listener, so the credential and SecureString sections exercise Negotiate's own encryption rather than a TLS transport. | closed |
| PSRP-32 | API | n/a | The convenience layer's scope | Open by choice, narrowed once. `psrp_client.h` covers connecting, running commands, the five streams, and now pipeline input: psrp_client_run_input for objects and psrp_client_run_bytes for the single-byte-array case. Input was added because the sequence it enables -- ordinary commands, one that consumes binary, then more ordinary commands on the same runspace -- is a real use rather than a guess, and it is verified live including that the session survives the input command. Still not covered: host callbacks, disconnect and reconnect, secure strings and command metadata, all supported at the low level and exercised by the live suite. The escape hatch remains psrp_client_session and psrp_client_transport, and the layer still adds no entry point into the state machine. The remaining known limit is that input is sent as one batch before the wait begins, so a command that must be fed in response to what it emits still needs the low-level API; streaming output through a callback is the other likely addition. | open |
| PSRP-33 | Tooling | n/a | libxml2 wrote parse diagnostics to a caller's stderr | Closed, and found by the Linux port paying for itself immediately. libxml2 prints parse errors to stderr by default, so every malformed-input test -- which is to say every test of what a hostile or broken server sends -- printed a parser error the caller never asked for. XmlLite is silent, so the two backends disagreed on observable behaviour, and a library writing to a caller's terminal is wrong regardless. Fixed with XML_PARSE_NOERROR | XML_PARSE_NOWARNING; the caller learns about malformed input from PSRP_ERR_XML as before. The CI memcheck job now treats any stderr output as a failure, so it cannot come back quietly. | closed |
| PSRP-34 | API | n/a | The public API took wchar_t for connection and credentials | Closed. `psrp_wsman_config_t` and `psrp_client_config_t` now take UTF-8 `char *` like every other string in the library; wchar_t survives only inside the two Windows transport files, which convert once and own the result. That also removed a latent hazard: WSManCreateSession keeps the credential pointers it is given rather than copying them, so the old code made the caller's config lifetime part of this library's contract without saying so. Done now rather than later because a POSIX transport should not inherit a Windows-ism, and the change is cheap while the userbase is one. Two bugs were introduced and caught doing it, both worth recording: the credential copies were first freed at the top of psrp_transport_free, which frees them while WSMan is still closing the session that holds the pointers; and the mechanical edit of the interop tests removed `free(user)` but not `free((void *)cfg.username)`, which then freed a getenv pointer and corrupted the heap. The second one hung two suites for ten minutes and was invisible until the traces showed all seven rounds completing before the stall -- the failure was in cleanup, not in anything the test was testing. | closed |
| PSRP-35 | Transport | 3.1.5.3 | A curl/GSS-API transport for Linux | In progress. A Linux client now opens a RunspacePool, runs a pipeline and reads its output back from a live server: NTLM authenticated, both directions encrypted, protocolversion 2.2 negotiated. Implemented: create, command, send, send priority, receive, signal, close. Still absent and saying so with PSRP_ERR_UNSUPPORTED: disconnect, reconnect, connect. Shell enumeration is not ported at all -- it uses the WSMan COM automation interface on Windows and would need WS-Enumerate here. Envelope findings, each of which reads as something other than what it is: the ShellId and CommandId in every selector must be the ones the SERVER returns, not the ones requested, or the request comes back as "the shell was not found on the server"; and the OptionSet carrying protocolversion needs s:mustUnderstand, without which WinRM calls the envelope's OptionSet invalid or duplicated. | open |
| PSRP-36 | Transport | 3.1.5.3.3 | PSRP-08 does not apply to the curl transport | Recorded because it narrows an existing deviation rather than adding one. PSRP-08 notes that 3.1.5.3.3 requires an empty rsp:Command element and that the Win32 WSMan client will not send one -- it rejects an empty command line client-side with 0x80338180, so the Windows transport sends a single space. The curl transport writes the envelope itself, so it sends a genuinely empty <rsp:Command></rsp:Command>, and the server accepts it: verified by a pipeline that runs and returns its output. So the deviation is a limitation of the Windows client library, not of this library, and it is absent on Linux. | closed |
| PSRP-37 | API | n/a | The transport interface conflated PSRP's needs with WinRM's | Closed. One header declared three different things: the byte-pipe contract the sans-IO core needs, the disconnect/reconnect/connect operations only WS-Management implements, and shell enumeration, which has nothing to do with PSRP at all. Split into psrp_transport.h (generic, no constructor -- every carrier is built differently) and psrp_winrm.h (configuration, constructor, the WSMan-only operations, enumeration). No behaviour change; both toolchains, six live Windows suites and the Linux live test pass unchanged. It also fixes a real defect rather than only tidying: enumeration was declared on every platform and defined only on Windows, so a Linux caller got an unresolved symbol at link time. It is now declared where it is implemented, and the same call fails to compile at the call site naming the function. Not done, deliberately: the vocabulary is still WS-Management's -- "shell" and "command" -- because PSRP over SSH, the only other carrier PowerShell speaks, has neither, and renaming for a transport that does not exist would be inventing an abstraction on speculation. Also not done: this stays one library. Two artifacts would double packaging and versioning for a userbase of one, and the separation that matters is the interface. | closed |
| PSRP-06 | Server role | 3.2 | Server-side protocol details | **Out of scope** by sign-off: client only. Recorded here so the exclusion is explicit rather than an oversight. | wontfix |

## Closed

_(none yet)_
